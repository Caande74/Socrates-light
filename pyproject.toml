[project]
name = "unified-runtime-api-v1"
version = "0.1.0"
description = "Runtime API for memory and learning behind AI-ledningsteamet"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
